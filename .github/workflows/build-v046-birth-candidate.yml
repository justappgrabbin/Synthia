name: Synthia v0.4.6 Duplicate Workflow Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Canonical workflow notice
        run: |
          echo "The canonical Synthia birth build is .github/workflows/build-v045-birth-candidate.yml"
          echo "This duplicate workflow is intentionally disabled from push builds."
